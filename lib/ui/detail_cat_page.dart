import 'package:flutter/material.dart';

class DetailCatPage extends StatelessWidget {
  const DetailCatPage({super.key});

  static const String namePage = 'detail_cat';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
